import { createStyles,css } from "antd-style";

export const useStyles=createStyles({
    image:css`
    border:2px solid red;
    background-position: calc(-400px + max(50%, -30px)) calc(0px + max(50%, 0px));
    
    
    `
})